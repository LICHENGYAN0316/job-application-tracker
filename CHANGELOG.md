# 职序 · 更新与故障修复记录

本文件保存已核实的线上故障、修复边界与验证结果，便于回溯产品决策和发布质量。时间使用墨尔本当地时间，并标注时区。更早的产品演进见 [README](./README.md#产品迭代日志) 与 Git 提交历史。

## 记录规范

每次影响用户体验、身份状态、数据安全或智能助手行为的修复，至少记录：

- 发现时间与版本；
- 故障现象及影响范围；
- 已确认的原因；
- 修复内容与未改变的边界；
- 自动化测试、Lint、构建及必要的界面检查结果；
- GitHub 与线上发布状态。

公开记录不包含真实账号、邮箱、求职正文、备份数据、API Key、OAuth Secret、Cookie、管理员 ID 或对话正文。

---

## 2026-09-01 16:12 AEST（UTC+10）— 登录状态保护与一键切断

**对应代码提交：** `d514805`

### 故障现象

- ChatGPT 与 GitHub 登录状态同时存在时，系统会因无法唯一确认账号而阻止打开工作台。
- 原恢复页只有“重新连接”；用户无法判断应退出哪个登录入口。
- 旧退出流程依赖已识别的登录方式，异常状态下可能只退出其中一种身份。
- 无法确认账号时没有活动缓存键，旧清理逻辑无法删除本设备上遗留的其他职序账号缓存。
- 保护说明在窄屏上出现“账／号”单字断行，阅读不自然。

### 影响与数据边界

- 工作台在身份确认前保持关闭，因此没有显示其他账号的本机缓存。
- 未发现云端公司、岗位或招聘流程被删除、覆盖或跨账号读取。
- 本次问题属于登录恢复路径和界面可用性问题，不是云端数据删除故障。

### 原因

- 异常状态无法可靠取得当前认证提供方，而原退出函数只按一个提供方执行。
- 本机清理只处理当前活动账号键，没有覆盖“账号尚未确认”的恢复场景。
- 原正文较长，且没有为中文保护文案设计稳定的换行结构。

### 修复内容

- 两个身份保护页面统一提供“重新连接”和“一键切断并重新登录”。
- 一键切断只删除职序自己的旧缓存键及 `career-pipeline-user-state-v2:*` 账号缓存，不使用 `localStorage.clear()`，不影响同域其他数据。
- 先清除 GitHub 本机会话与未完成 OAuth 状态，再进入 ChatGPT 退出流程，解决双登录冲突。
- GitHub 数据库暂时不可用时仍清除本设备 HttpOnly Cookie；跨站请求仍被 Origin 校验拒绝。
- 客户端仅在收到服务端本机会话清理证明后继续，并设置 10 秒超时；弱网或异常响应会停留并提示重试。
- 全流程不会请求删除云端数据的接口。
- 保护文案改为两条完整短句，并补充手机安全区、响应式内边距、禁用状态和无障碍忙碌提示。

### 验证结果

- 104 项自动化测试通过。
- ESLint 通过。
- Vinext 生产构建通过。
- 同源退出、跨站拒绝、数据库故障仍清 Cookie、账号缓存键范围均有自动化验证。
- 320、375、390 px 手机宽度与桌面卡片布局完成发布前审查，无单字断头或横向溢出阻塞。

### 发布状态

- GitHub `main`：已保存本次修复代码与公开说明。
- 线上站点：以本文件所在最终提交重新构建和发布。

---

## 2026-09-01 15:37 AEST（UTC+10）— 智能助手状态一致性与查询匹配

**对应提交：** `f8052c2`

### 故障现象

- 部分智能助手状态、查询过滤和实体匹配结果与工作台真实数据不一致。
- 某些新增、修改、删除意图的 Tool 参数不完整时，操作预览或反馈统计不能稳定反映真实执行结果。

### 修复内容

- 统一智能助手读取的数据版本和状态口径。
- 加固查询过滤、唯一实体匹配、Tool 参数校验及新增／修改／删除提案解析。
- 只有数据库真实执行成功后才显示完成；冲突、取消和错误反馈进入对应统计口径。
- 使用匿名 9 公司、21 岗位形状回归数据验证完整查询和字段保持。

### 发布状态

- 自动化测试、Lint 与生产构建通过后发布。

---

## 2026-09-01 13:15 AEST（UTC+10）— 相对日期时区错误

**对应提交：** `359f1af`

### 故障现象

- “今天”“明天”等相对日期未始终按请求者所在地计算，可能生成错误的投递日期。

### 修复内容

- 读取请求者浏览器提供的 IANA 时区，并由服务端重新解析相对日期。
- IANA 时区不可用时使用浏览器 UTC 偏移；无法安全确定日期时停止提案，不猜测日期。
- 补充跨时区、显式日期和缺失时区的自动化测试。

### 发布状态

- 自动化测试、Lint 与生产构建通过后发布。

