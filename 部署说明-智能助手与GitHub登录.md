# 职序：智能助手与 GitHub 登录部署说明

这份说明用于多用户联网版。源码可以公开，但任何密钥都只放在部署平台的服务端 Secret 中，不写进代码、GitHub、聊天或浏览器。

## 一、准备豆包通用 API

1. 打开火山方舟控制台的“API Key”页面。
2. 使用“通用 API Key”，不要使用 Agent Plan Key。
3. 新建一把仅供职序使用的 Key；创建后不要截图、不要粘贴到聊天。
4. 在模型页面确认这把通用 Key 可以调用的豆包 Mini 模型，并复制控制台显示的完整模型 ID。
5. 在部署平台的服务端 Secrets / Environment Variables 中填写：

| 名称 | 填写内容 | 是否敏感 |
| --- | --- | --- |
| `ARK_API_KEY` | 新建的通用 API Key | 是，必须使用 Secret |
| `ARK_MODEL_ID` | 控制台显示的完整模型 ID | 否 |

不要给变量添加 `NEXT_PUBLIC_` 前缀。这样密钥只会留在服务端。

## 二、设置唯一初始管理员

1. 使用你的长期 ChatGPT 账号登录正式网站。
2. 发布时由维护者从当前账号已认证的服务端响应中读取稳定用户 ID；不要在聊天中发送这个 ID。
3. 把它保存为服务端变量 `ADMIN_CHATGPT_USER_ID`。

只有同时满足“ChatGPT 身份”和“稳定 ID 完全匹配”的账号才是管理员。GitHub 登录永远不会自动成为管理员。

## 三、创建 GitHub OAuth 登录

1. 打开 GitHub：`Settings → Developer settings → OAuth Apps → New OAuth App`。
2. Application name 填写“职序”。
3. Homepage URL 填写正式网站地址：

   `https://job-application-desk-lcy.plupluto.chatgpt.site`

4. Authorization callback URL 必须精确填写：

   `https://job-application-desk-lcy.plupluto.chatgpt.site/api/auth/github/callback`

5. 不要开启 callback wildcard，不要开启 Device Flow。
6. 创建后把下面三个值放到部署平台：

| 名称 | 填写内容 | 是否敏感 |
| --- | --- | --- |
| `GITHUB_CLIENT_ID` | OAuth App 的 Client ID | 否 |
| `GITHUB_CLIENT_SECRET` | OAuth App 的 Client Secret | 是，必须使用 Secret |
| `PUBLIC_APP_ORIGIN` | `https://job-application-desk-lcy.plupluto.chatgpt.site` | 否 |

本系统不申请仓库权限。GitHub token 只在登录回调中读取一次数字用户 ID，随后立即丢弃；数据库只保存登录会话的哈希。

### Cloudflare 边界限流建议

在 Cloudflare WAF 中为 GitHub 登录入口单独建立按客户端 IP 计数的 Rate Limiting 规则，不要只依赖应用内限制：

- `/api/auth/github/start`：建议起始值为每 IP 每 60 秒 10 次。
- `/api/auth/github/callback`：建议起始值为每 IP 每 60 秒 20 次。
- 超限后优先使用 Managed Challenge；如攻击持续，再改为短时间 Block。上线后根据真实共享网络和移动网络流量调整阈值，避免误伤同一出口 IP 的正常用户。

同样应对 Agent 接口建立边界限流，避免恶意请求增加 D1 写入、污染质量统计或消耗模型调用。可从以下值开始，再按真实流量调整：

- `/api/agent/query`：每 IP 每 60 秒 20 次；超限优先 Managed Challenge。
- `/api/agent/feedback`：每 IP 每 60 秒 30 次。
- `/api/agent/action/*/confirm`：每 IP 每 60 秒 20 次。
- `/api/agent/action/*/cancel`：每 IP 每 60 秒 30 次。
- `/api/agent/action/*/feedback`：每 IP 每 60 秒 30 次。
- `/api/agent/status`：每 IP 每 60 秒 120 次。
- `/api/admin/agent`：每 IP 每 60 秒 60 次。

边界限流是正式开启付费模型前的必做项，不能被“普通账号 5 次 / 24 小时”替代。

### 认证数据保留

- OAuth state 的功能有效期为 10 分钟，已消费或过期记录会在后续认证请求中尽力清理。
- GitHub 登录会话有效期为 30 天，已撤销或过期记录同样会尽力清理；GitHub provider token 从不持久化。
- “尽力清理”不等于精确时点删除。如合规要求严格的最长保留时间，应另外配置每日定时清理，并确保日志不记录 OAuth `code`/`state`、Cookie 或 access token。

## 四、首次上线顺序

1. 先完成所有环境变量和 Secret 配置。
2. 发布新版。
3. 用你的 ChatGPT 管理员账号进入“职程领航 → 管理面板”。
4. 确认默认状态为“智能调用已关闭”。
5. 完成一轮测试后，再点击“开启智能调用”。

普通账号初始默认在滚动 24 小时内最多成功使用 5 次；管理员可以在管理面板把全体普通账号默认额度设为 0–100 次，也可以给单个用户设置独立额度或恢复全体默认。额度变更后会立即显示，并在后台校准。网络错误、超时、服务端报错和解析失败不扣次数。管理员不限次数，但仍记录 Token。全局关闭对管理员同样生效。

### 调用与 Token 边界

- 当前版本使用“普通账号可配置额度 / 滚动 24 小时 + 单用户停用 + 管理面板全局开关”控制调用量。
- 管理面板只展示 Token 用量，不估算人民币费用。
- 管理员不限次数，因此仍建议在火山方舟控制台设置账户预算或余额告警；异常时应先用管理面板“紧急关闭”。

### Agent Tool 增删改查的安全边界

- Agent 开放当前账号范围内的只读查询，以及单条“新增公司、新增岗位、修改公司、修改岗位、删除公司、删除岗位”提案；不开放批量写操作或流程记录修改。
- 若目标公司不存在，Agent 会先展示“创建公司并新增岗位”的组合提案；缺失字段显示“未填写”。
- 模型通过严格工具结构生成提案，但没有数据库权限，也不能宣告操作已完成。
- 查询不会修改数据；新增、修改和删除第一次只展示操作提案，第二次必须由用户在独立确认界面再次确认。删除使用红色风险区，并优先提供“取消”按钮。
- 服务器会再次验证当前账号、目标唯一性、重复记录、确认凭证和数据版本。零匹配或多匹配只会澄清，不会猜测。
- 只有数据库返回真实写入成功后页面才显示成功；并发点击、重复请求、过期提案和版本冲突都不会重复执行。
- 提案 10 分钟后失效，无法再执行；取消、完成或失败时立即清空最少字段，未处理的过期提案会在该用户下次打开网站或使用助手时清空。若需要严格的最长保留时间，应再配置定时清理。长期统计只保留匿名运行事件，不保存公司名、岗位名、备注、问题或回答正文。

### Evaluation 指标口径

管理面板使用中文表格展示“分类、指标、当前值、样本量、统计口径、状态”。任何比例的分母为 0 时都显示“暂无样本”，不能显示成误导性的 `0%`。

- **技术成功率**：正常返回数 ÷（正常返回 + 技术失败）。
- **用户确认任务解决率**：用户点击“已解决”或将执行结果评价为“操作正确”的任务数 ÷ 已评价任务数；同一匿名会话只计一次。这是用户反馈代理，不冒充客观准确率。
- **一轮解决率**：一轮内被用户确认解决的任务 ÷ 已评价任务数。
- **反馈覆盖率**：已评价任务 ÷ 可评价成功任务；必须与任务解决率一起看。
- **工具参数校验通过率**：通过服务端结构与取值校验的工具参数 ÷ 全部工具提案；不能改名为“参数完全匹配率”。
- **工具执行成功率**：数据库真实执行成功次数 ÷ 已确认并尝试执行次数。
- **多候选请求安全澄清率**：零匹配或多匹配写请求中，先澄清且未提前执行的占比。
- **用户确认误操作率**：用户标记“操作有误”的执行操作 ÷ 全部已评价操作。
- **未经确认执行次数**：缺少二次确认却发生写入的次数，目标必须为 0。
- **版本冲突率、重复新增拦截次数**：用于观察并发安全与去重效果。
- **完成任务平均轮数**：用户最终确认“已解决”的任务中，每个随机任务会话的成功调用数。
- **平均响应时长与 95 分位响应时长**：只统计拥有真实正数耗时记录的成功调用；迁移前缺少耗时的历史记录不进入分母。
**意图准确率、工具选择准确率、参数完全匹配率** 不在在线面板展示，因为它们必须使用人工标注的金标评测集。后续应建立版本化离线评测集，再将模型结果与预期意图、工具和参数逐条比较。

调用元数据目前不会自动到期删除。正式对外运营前，建议确定保留期（例如 90 天），再增加每日定时清理；清理不影响用户的求职数据。

## 五、上线验收

- ChatGPT 和 GitHub 即使邮箱相同，也得到两套独立数据。
- 普通账号第 6 次请求被拦截，基础助手仍可使用。
- 管理员修改全体默认额度或单用户额度后，对应用户界面显示相同额度；设为 0 时智能调用不可用，但基础助手和网站其他功能正常。
- 网络失败后已用次数不增加。
- 管理面板不显示岗位内容或对话正文。
- 质量面板在没有有效分母时显示“暂无样本”，不显示误导性的 `0%`。
- 查询只读取当前账号；新增、修改和删除只先生成提案，不点击第二次确认时数据完全不变。
- 已有公司新增岗位时，地点、链接与投递日期允许为空并显示“未填写”；公司不存在时先展示创建公司并新增岗位的组合提案。
- 删除确认界面使用红色风险提示；过期提案、重复点击、错误确认凭证和数据版本冲突均不执行。
- 操作执行成功后可以评价“结果正确 / 操作有误”，管理面板只增加匿名统计，不出现公司或岗位正文。
- 浏览器网络请求、网页源码和 GitHub 仓库中均找不到豆包 Key、GitHub Secret 或管理员 ID。
- 手机端职程领航为全屏页面，关闭后背景位置不跳动。
- 全局紧急关闭后，所有账号都不能调用模型，网站其他功能仍正常。

## 六、密钥泄露时

1. 先在管理面板执行“紧急关闭”。
2. 到对应平台立即禁用旧 Key / Secret。
3. 创建新值并更新服务端 Secret。
4. 重新发布并完成验收后再开启智能调用。
